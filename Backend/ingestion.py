"""
This script is the logic core for the ingestion feature. It is called by delivery.
"""

import chromadb as chroma


def ingest_document() -> bool:
    """
    Ingests a document uploaded via the UI.

    Args:
        

    Returns:
        
    """